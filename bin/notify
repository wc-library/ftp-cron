#!/bin/bash
script_dir="$(dirname "${BASH_SOURCE[0]}")"
source "${script_dir}/.config"
##########################################

subject="$1"
message="$2"
files="${@:3}"

echo "$message" | mail -s "$subject" -r "$email_from" "$email_to" -a $files

