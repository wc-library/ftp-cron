#!/bin/bash
orig_dir=`pwd`
cd "$(dirname "${BASH_SOURCE[0]}")"
source ".config"
cd $proj_dir/data/.mirror

if [ -z "$lftp_script" ]; then
    bin/exec "mirror $sync_args"
else
    bin/exec "$lftp_script mirror $sync_args"
fi

cd $orig_dir
